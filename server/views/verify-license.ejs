<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Verify License</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style> body { font-family: 'Inter', sans-serif; } </style>
</head>
<body class="bg-gray-50"
      data-contract-address="<%= contractAddress || '' %>"
      data-contract-name="<%= contractName || '' %>"
      data-hiro-api="<%= hiroApi || 'https://api.testnet.hiro.so' %>">

  <main class="max-w-2xl mx-auto py-10 px-4">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">Verify your license</h1>
    <p class="text-gray-600 mb-6">Connect your wallet and we’ll find your license automatically.</p>

    <div id="status" class="hidden p-3 rounded text-sm"></div>

    <div class="flex items-center gap-3 mb-6">
      <button id="connect"
              class="px-4 py-2 rounded-lg text-white bg-indigo-600 hover:bg-indigo-700 focus:ring-2 focus:ring-indigo-500">
        Connect Leather
      </button>
      <span id="connected-addr" class="text-sm text-gray-600"></span>
    </div>

    <!-- Scanning / Results -->
    <div id="scan-box" class="hidden bg-white rounded-xl shadow p-5 space-y-3">
      <div class="flex items-center justify-between">
        <h2 class="text-lg font-semibold text-gray-900">Detected licenses</h2>
        <span id="scan-note" class="text-xs text-gray-500"></span>
      </div>
      <div id="licenses" class="space-y-2"></div>
      <div id="none" class="hidden text-gray-600 text-sm">No licenses found for this wallet (in recent IDs).</div>
    </div>

    <!-- Success state -->
    <div id="success" class="hidden mt-8 bg-green-50 border border-green-200 rounded-xl p-6">
      <h3 class="text-xl font-semibold text-green-800 mb-2">Software verified — welcome aboard! 🎉</h3>
      <dl class="text-sm text-green-900 grid grid-cols-2 gap-4">
        <div>
          <dt class="font-medium text-green-700">Software</dt>
          <dd id="s-name" class="font-semibold"></dd>
        </div>
        <div>
          <dt class="font-medium text-green-700">License ID</dt>
          <dd id="s-id" class="font-mono"></dd>
        </div>
        <div>
          <dt class="font-medium text-green-700">Duration</dt>
          <dd id="s-days"></dd>
        </div>
        <div>
          <dt class="font-medium text-green-700">Redeemed at</dt>
          <dd id="s-redeemed"></dd>
        </div>
      </dl>
      <p class="mt-4 text-xs text-green-700">
        Tip: you can bookmark this page to quickly check your license again later.
      </p>
    </div>
  </main>

  <script type="module" src="/js/verify-client.js"></script>
</body>
</html>
