<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Claim Your License</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    /* soft spotlight bg */
    .bg-spot:before{
      content:""; position:fixed; inset:-20% -20% auto -20%; height:70vh; z-index:-1;
      background: radial-gradient(60% 60% at 50% 0%, rgba(79,70,229,.12), transparent 70%);
      pointer-events:none;
    }
  </style>
  <link rel="icon" href="/favicon.ico">
</head>

<body class="bg-gray-100 bg-spot min-h-screen flex items-center justify-center">
  <div class="w-full max-w-lg p-8 space-y-6 bg-white/90 backdrop-blur rounded-2xl shadow-xl border border-gray-100 text-center">
    <!-- header -->
    <div class="space-y-2">
      <div class="inline-flex items-center gap-2 px-2.5 py-1.5 rounded-full text-xs bg-indigo-50 text-indigo-700 border border-indigo-100">
        <span class="w-2 h-2 rounded-full bg-indigo-500"></span>
        DecentraKey • Testnet
      </div>
      <h1 class="text-3xl font-bold text-gray-900">Claim Your License</h1>
      <p class="text-gray-500">You've been issued a software license for:</p>
    </div>

    <!-- summary card (unchanged data) -->
    <div class="bg-gray-50 border border-gray-200 rounded-xl p-6 text-left space-y-4">
      <div>
        <p class="text-sm font-medium text-gray-500">Software</p>
        <p class="text-lg font-semibold text-gray-900" id="software-name"><%= license.softwareName %></p>
      </div>
      <div class="grid grid-cols-2 gap-4">
        <div>
          <p class="text-sm font-medium text-gray-500">License Duration</p>
          <p class="text-lg font-semibold text-gray-900" id="license-duration"><%= license.licenseDurationDays %> Days</p>
        </div>
        <div>
          <p class="text-sm font-medium text-gray-500">Intended Wallet</p>
          <p class="text-sm font-mono text-gray-700 break-all" id="wallet-address"><%= license.customerWalletAddress %></p>
        </div>
      </div>
    </div>

    <!-- your original status nodes (kept as-is) -->
    <div id="status-message" class="hidden p-4 rounded-lg text-sm"></div>
    <div id="popup-help" class="hidden"></div>

    <!-- data payload (unchanged) -->
    <div id="license-data"
         data-voucher-id="<%= license.voucherId %>"
         data-contract-address="<%= contractAddress %>"
         data-contract-name="<%= contractName %>"
         data-claim-token="<%= license.claimToken %>"
         data-intended-address="<%= license.customerWalletAddress %>">
    </div>

    <!-- tiny debug line (unchanged content) -->
    <p class="text-xs text-gray-400">
      Contract: <%= contractAddress %>.<%= contractName %> | Voucher: <%= license.voucherId %>
    </p>

    <!-- already-claimed alert (unchanged logic) -->
    <% if (license.isClaimed) { %>
      <div class="p-4 rounded-lg bg-green-50 border border-green-200 text-green-800 text-sm mb-3">
        This license was already claimed on
        <%= license.redeemedAt ? new Date(license.redeemedAt).toLocaleString() : 'a previous attempt' %>.
        Tx: <span class="font-mono"><%= license.redeemTxId || 'unknown' %></span>
      </div>
    <% } %>

    <!-- primary CTA (unchanged id/logic/classes preserved) -->
    <button id="claim-button"
      class="w-full flex items-center justify-center gap-2 py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white
      <%= license.isClaimed ? 'bg-gray-400 cursor-not-allowed' : 'bg-indigo-600 hover:bg-indigo-700' %> focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition-all duration-300"
      <%= license.isClaimed ? 'disabled' : '' %>>
      <!-- icon -->
      <svg class="w-4 h-4 text-white" viewBox="0 0 24 24" fill="none" aria-hidden="true">
        <path d="M12 5v9m0 0l3-3m-3 3l-3-3M5 19h14" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <%= license.isClaimed ? 'Already Claimed' : 'Connect Wallet & Claim License' %>
    </button>

    <!-- your second set of helper/status nodes (kept as-is) -->
    <div id="status-message" class="hidden p-4 rounded-lg text-sm"></div>
    <div id="popup-help" class="hidden bg-yellow-50 border border-yellow-200 rounded-lg p-4 text-sm text-left">
      <p class="text-yellow-800 font-medium">Wallet popup blocked?</p>
      <ul class="text-yellow-700 mt-2 space-y-1 text-xs list-disc list-inside">
        <li>Check if popups are blocked for this site</li>
        <li>Try clicking the wallet extension icon</li>
        <li>Refresh the page and try again</li>
      </ul>
    </div>

    <!-- footer microcopy -->
    <p class="text-[11px] text-gray-400">
      Having trouble? Ensure your Leather wallet is on <span class="font-medium text-gray-600">Testnet</span>.
    </p>
  </div>

  <!-- Only this script should run (unchanged) -->
  <script type="module" src="/js/claim.js"></script>
</body>
</html>
